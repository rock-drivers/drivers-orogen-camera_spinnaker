# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_spinnakerTaskLib)
ADD_LIBRARY(${CAMERA_SPINNAKER_TASKLIB_NAME} SHARED
    ${CAMERA_SPINNAKER_TASKLIB_SOURCES})
add_dependencies(${CAMERA_SPINNAKER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CAMERA_SPINNAKER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${CAMERA_SPINNAKER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_SPINNAKER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_SPINNAKER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${CAMERA_SPINNAKER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${CAMERA_SPINNAKER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_SPINNAKER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_SPINNAKER_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_spinnaker)

